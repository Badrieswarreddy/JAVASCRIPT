var arr=new Array("HTML","CSS","JavaScript");
console.log(arr);
//Another way to intialize elements
 var courses=new Array(3);
 courses[0]="HTML";
 courses[1]="CSS";
 courses[2]="JavaScript";
 console.log(courses);
 //Accessing the elements by using the for loop
 for(i=0;i<arr.length;i++)
 {
    console.log(courses[i]);
 }